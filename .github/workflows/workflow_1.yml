run-name: Workflow 1 is running
name: Workflow 1
on:
  workflow_call:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Check branch
        run: echo "echo ${GITHUB_REF##*/}"
      - uses: actions/checkout@v3
        with:
            fetch-depth: 0